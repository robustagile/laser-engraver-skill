# ---------------------------------------------------------------------------
# No private working area is excluded here, deliberately: this repository holds
# no machine records, recipes or burn readings at all (R-D4). Real work happens
# in a separate installation, and only knowledge comes back — never data.
# ---------------------------------------------------------------------------

# ---------------------------------------------------------------------------
# Generated engraving output — see R-G4: output is disposable, the generator
# is the source of truth.
#
# Deliberately NOT ignoring *.lbrn: probe files and golden reference files are
# .lbrn and must stay under version control (R-G12, R-G13, R-N1).
# ---------------------------------------------------------------------------
out/
*.lbrn.tmp

# ---------------------------------------------------------------------------
# .NET build artifacts
# ---------------------------------------------------------------------------
[Bb]in/
[Oo]bj/
[Dd]ebug/
[Rr]elease/
artifacts/
TestResults/
*.user
*.suo
*.nupkg
project.lock.json
.vs/

# ---------------------------------------------------------------------------
# Editors and OS
# ---------------------------------------------------------------------------
.idea/
*.swp
.DS_Store
Thumbs.db
