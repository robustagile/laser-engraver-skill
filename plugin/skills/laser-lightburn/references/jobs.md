# Jobs

TODO: production, the file side (R-W3.1 to R-W3.8). The material side - which recipe, and
why - belongs to `laser-machines`.

## Mode 1 - generated from a description

TODO: R-W3.4.1 - built within what the writer can actually emit, saying plainly where that
boundary runs rather than approximating past it.

## Mode 2 - drawn by the user

TODO: R-W3.4.2 - the artwork is the user's; the parameters are what they are stuck on. Most
real work is this mode. Delivery is text (see SKILL.md).

## Positioning

TODO: R-W3.2, R-W3.3 - origin, where the workpiece sits, framing.

## The same geometry on more than one layer

TODO: R-W3.8 - cut to depth, then a second pass over identical geometry for contrast.
Registration is free in one generated file and a real problem done by hand, which is why it
belongs to the generator.

## Generation-time checks

TODO: R-C3 - a regulatory acceptance criterion (minimum character height, minimum depth) is
checked when the file is generated, with the verification method stated.
