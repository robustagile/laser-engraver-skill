# Building the recipe base

TODO: workflow 2 (R-W2.1 to R-W2.4) and the recipe lifecycle (R-R14). No recipes ship;
the catalogue of which recipes are needed does (R-R12).

## The coverage view

TODO: R-R13 - the catalogue minus this machine's recipes is the work list. Priority means
"in what order does a new machine need this" (R-R19).

## Researching candidates

TODO: R-W2.1 - this exact machine first, then the laser type and power class.

## Seeding a candidate with provenance

TODO: R-R7, R-R15 - full provenance at the moment of seeding, because there is no verified
data at all when a machine is registered.

## Calibrating a candidate

TODO: one recipe at a time. Test card -> burn -> read the result -> refine.
-> `laser-lightburn/references/test-cards.md`.

## Acceptance criteria

TODO: R-R5 - every recipe carries what it must achieve and how that is verified. The
catalogue entries already state theirs; they are proposals until a burn.

## Promoting a candidate to verified

TODO: R-R14 step 5. What must be true, and what is recorded.

## Recipes that can never be verified

TODO: R-R18 - a material the owner does not have keeps candidate provenance indefinitely and
shows in the coverage map as a gap, not as done.
