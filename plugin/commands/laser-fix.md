---
description: Diagnose a mark that came out wrong, starting from the symptom
argument-hint: [the symptom - "came out grey", "burned through", "nothing visible"]
---

Workflow 4 (R-W4). TODO: start from the symptom, not the parameter. Establish what was set, on
what material, with which recipe; then work back. Route into `/laser-recipes` when the answer
is that no calibrated recipe exists. Uses `laser-machines`.
