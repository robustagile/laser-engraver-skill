---
description: Discuss a job and produce the LightBurn file, or the parameters for one drawn by hand
argument-hint: [what is being made, and on what material]
---

Workflow 3 (R-W3). TODO: establish the mode (generated, or drawn by the user), choose the
recipe, and say so when there is no verified recipe for the material rather than proceeding -
offering to go and calibrate one. Uses both skills: `laser-machines` chooses the recipe,
`laser-lightburn` writes the file or the parameter block.
