---
description: Register a laser machine, configure LightBurn for it, and calibrate it
argument-hint: [machine name, or nothing to pick up where onboarding stopped]
---

Workflow 1 (R-W1). TODO: locate the data store, read what is there, and branch on the state
found - no machine yet, `registered`, `configured` or `calibrated`. Use the `laser-machines`
skill.
